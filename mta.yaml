ID: cap-jenkins-demo
_schema-version: "3.1"
version: 1.0.0

modules:
  - name: cap-jenkins-srv
    type: nodejs
    path: .
    parameters:
      memory: 256M
    build-parameters:
      builder: npm

resources: []